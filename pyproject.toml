[project]
name = "agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "llm>=0.30",
    "llm-anthropic>=0.24",
    "pydantic>=2.12.5",
]

[dependency-groups]
dev = [
    "ruff>=0.15.10",
]
